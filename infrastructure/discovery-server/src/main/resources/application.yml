server:
  port: 8761
spring:
  application:
    name: discovery-server

logging:
  level:
    root: INFO

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
